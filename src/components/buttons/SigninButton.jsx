import React from "react";

const SigninButton = () => {
  return (
    <button type="submit" className="text-text-primary text-sm font-semibold bg-text-link w-full py-1 rounded-lg">
      Log in
    </button>
  );
};

export default SigninButton;
